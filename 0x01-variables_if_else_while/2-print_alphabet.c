#include <stdio.h>

/**
 * main - prints the numbers from 00 to 99
 * Return: Always 0 (success)
 */
int main(void)
{
	char alph=''a';
		while(alpha <='z')
		{
			putchar('%c',alpha);
			alpha ++;
			putchar('\n');
		}
		
	return (0);
}
